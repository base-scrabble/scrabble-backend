console.log('Blockchain fields are managed via Prisma migrations.');
console.log('Run the following to apply the merged schema with blockchain fields:');
console.log('npx prisma migrate dev --name add_blockchain_fields');
console.log('Or sync schema without migrations:');
console.log('npx prisma db push');